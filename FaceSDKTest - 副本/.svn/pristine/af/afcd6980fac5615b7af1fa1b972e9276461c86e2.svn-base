import os
import shutil
index = 1
alllist=os.listdir("img/")
for i in alllist:
    oldname = "img/" + i + "/1.jpg"
    newname = "image/" + str(index) + ".jpg"
    index = index + 1
    shutil.copyfile(oldname,newname)
